# Build artifacts
target/
*.o
*.a
*.so
*.dylib

# IDE
.vscode/
.idea/
*.swp
*.swo

# Agent tooling
.claude/

# OS
.DS_Store
Thumbs.db

# Worktrees
.worktrees/

# Rust
Cargo.lock
# ...but the Rust workspace's is tracked, so CI and every developer build
# resolve the same dependency versions for a given commit.
!rust/Cargo.lock
.worktrees/
