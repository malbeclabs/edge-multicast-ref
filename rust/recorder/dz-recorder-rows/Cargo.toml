[package]
name = "dz-recorder-rows"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "The rows an archive derives into, and the derivation itself: pure, sink-agnostic, and exercised with no server"

[dependencies]
dz-edge-core = { path = "../../codec/dz-edge-core" }
dz-recorder-core = { path = "../dz-recorder-core" }
dz-recorder-archive = { path = "../dz-recorder-archive" }
dz-recorder-loss = { path = "../dz-recorder-loss" }
dz-recorder-replay = { path = "../dz-recorder-replay" }
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
